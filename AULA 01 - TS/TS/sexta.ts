// Nesse caso, como no enunciado fala sobre clique, então eu entendi que são alerts que aparecem quando abre no navegador
// Ao invés de console.log.
// Comentário extra: aqui não teve muito o que se fazer, apenas definir que a função não tem retorno e colocar a mensagem numa string
function fazPegadinha(): void {
  const mensagem: string = "Você foi enganada!";
  alert(mensagem);
  alert(mensagem);
  alert(mensagem);
  alert(mensagem);
  alert(mensagem);
  alert(mensagem);
  alert(mensagem);
  alert(mensagem);
  alert(mensagem);
}

fazPegadinha();
