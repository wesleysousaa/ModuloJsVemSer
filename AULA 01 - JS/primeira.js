const celsiusParaFahrenheit = (temperature) => {
  const celsiusTemperature = temperature;
  console.log(
    `${celsiusTemperature}°C equivalem a ${celsiusTemperature * 1.8 + 32}°F`
  );
};

celsiusParaFahrenheit(36);
